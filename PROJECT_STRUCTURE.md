# 프로젝트 구조 상세

## 📁 모듈 구성

```
gachamongddang/
├── mongddang-front/          # 프론트엔드
│   └── web/                 # React + TypeScript + Vite
│
├── mongddang-api/            # API 서버 (Spring Boot + Kotlin)
│   ├── src/
│   │   ├── main/
│   │   │   ├── kotlin/
│   │   │   │   └── com/meg/gachamongddang/server/
│   │   │   │       ├── ServerApplication.kt
│   │   │   │       ├── config/
│   │   │   │       │   └── WebConfig.kt
│   │   │   │       ├── controller/    # REST 컨트롤러
│   │   │   │       ├── service/        # 비즈니스 로직
│   │   │   │       └── repository/    # 데이터 접근
│   │   │   └── resources/
│   │   │       └── application.properties
│   │   └── test/
│   └── build.gradle.kts
│
├── mongddang-admin/          # 어드민 프론트엔드 (나중에 구현)
│   └── README.md
│
├── mongddang-papi/           # 어드민 API 서버 (나중에 구현)
│   └── README.md
│
├── common/                   # 공통 모듈
│   ├── src/
│   │   └── main/kotlin/
│   │       └── com/meg/gachamongddang/common/
│   │           ├── domain/            # 도메인 모델 (Entity)
│   │           ├── dto/               # DTO
│   │           ├── util/               # 유틸리티 클래스
│   │           └── exception/         # 공통 예외
│   └── build.gradle.kts
│
├── build.gradle.kts          # 루트 빌드 설정
├── settings.gradle.kts       # 모듈 설정
├── deploy.sh                 # 배포 스크립트
└── README.md
```

## 🔧 모듈별 상세

### 1. mongddang-front (프론트엔드)
- **경로**: `mongddang-front/web`
- **기술 스택**: React 19 + TypeScript + Vite + Tailwind CSS
- **포트**: 개발 서버 5173
- **API 연동**: `/v1/*` 경로로 mongddang-api 모듈과 통신

### 2. mongddang-api (API 서버)
- **경로**: `mongddang-api/`
- **기술 스택**: Spring Boot 3.5.5 + Kotlin + JPA
- **포트**: 8080
- **API 경로**: `/v1/*`
- **의존성**: `common` 모듈
- **기능**:
  - 프론트엔드 정적 파일 서빙
  - API 엔드포인트 제공
  - SPA 라우팅 처리

### 3. mongddang-admin (어드민 프론트엔드)
- **경로**: `mongddang-admin/`
- **상태**: 준비 중
- **예정**: 어드민용 프론트엔드 애플리케이션
- **API 연동**: `mongddang-papi` 모듈과 통신

### 4. mongddang-papi (어드민 API 서버)
- **경로**: `mongddang-papi/`
- **상태**: 준비 중
- **예정**: 어드민용 API 서버 (mongddang-admin 모듈과 통신)
- **의존성**: `common` 모듈

### 5. Common (공통 모듈)
- **경로**: `common/`
- **역할**: 
  - 도메인 모델 정의 (Entity, DTO 등)
  - 공통 유틸리티 클래스
  - 공통 상수 및 설정
  - 공통 예외 처리
- **사용처**: `mongddang-api`, `mongddang-papi` 모듈에서 공통으로 사용

## 🚀 빌드 및 실행

### 개발 환경

```bash
# 서버 실행
./gradlew :mongddang-api:bootRun

# 프론트엔드 실행
cd mongddang-front/web
npm run dev
```

### 프로덕션 배포

```bash
# 전체 빌드
./deploy.sh build

# 서버 시작
./deploy.sh start

# 상태 확인
./deploy.sh status
```

## 📝 모듈 간 통신

```
┌─────────┐         ┌─────────┐         ┌─────────┐
│  Front  │ ──API──>│mongddang│<───────>│ Common  │
│         │ /v1/*   │  -api   │         │         │
└─────────┘         └─────────┘         └─────────┘
                                              ▲
┌─────────┐         ┌─────────┐              │
│  Admin  │ ──API──>│mongddang│<──────────────┘
│         │         │  -papi  │
└─────────┘         └─────────┘
  (준비 중)          (준비 중)
```

### 통신 흐름
1. **Front → mongddang-api**: 사용자 요청을 `/v1/*` API로 전송
2. **Admin → mongddang-papi**: 관리자 요청을 mongddang-papi로 전송 (구현 예정)
3. **mongddang-api/mongddang-papi → Common**: 공통 도메인 모델 및 로직 사용

## 🔄 다음 단계

1. ✅ Front 모듈 - 완료
2. ✅ mongddang-api 모듈 - 완료
3. ✅ Common 모듈 - 완료
4. ⏳ mongddang-admin 모듈 - 구현 예정
5. ⏳ mongddang-papi 모듈 - 구현 예정
