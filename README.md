# 가차몽딩 프로젝트

가챠 및 소품샵 웹 애플리케이션

## 📋 프로젝트 개요

가차몽딩은 가챠 시스템과 소품샵 기능을 제공하는 웹 애플리케이션입니다.  
프론트엔드와 백엔드를 분리한 멀티 모듈 구조로 구성되어 있습니다.

## 🏗️ 모듈 구조

```
gachamongddang/
├── mongddang-front/     # 프론트엔드 (React + TypeScript)
├── mongddang-api/      # API 서버 (Spring Boot + Kotlin)
├── mongddang-admin/    # 어드민 프론트엔드 (준비 중)
├── mongddang-papi/     # 어드민 API 서버 (준비 중)
└── common/             # 공통 모듈 (도메인 모델, 공통 로직)
```

## 📦 모듈별 상세 설명

### 1. mongddang-front (프론트엔드)
- **경로**: `mongddang-front/web`
- **기술 스택**: React 19 + TypeScript + Vite + Tailwind CSS
- **포트**: 개발 서버 5173
- **역할**: 
  - 사용자 인터페이스 제공
  - 상품 조회, 장바구니, 주문 등 사용자 기능
  - **API 통신**: `mongddang-api` 모듈의 `/v1/*` 엔드포인트로 요청

### 2. mongddang-api (API 서버)
- **경로**: `mongddang-api/`
- **기술 스택**: Spring Boot 3.5.5 + Kotlin + JPA
- **포트**: 8080
- **역할**:
  - 프론트엔드용 REST API 제공 (`/v1/*`)
  - 프론트엔드 정적 파일 서빙
  - 비즈니스 로직 처리
  - 데이터베이스 연동
- **의존성**: `common` 모듈 사용

### 3. mongddang-admin (어드민 프론트엔드)
- **경로**: `mongddang-admin/`
- **상태**: 준비 중
- **역할**:
  - 관리자 인터페이스 제공
  - 상품 관리, 주문 관리 등 관리자 기능
  - **API 통신**: `mongddang-papi` 모듈로 요청

### 4. mongddang-papi (어드민 API 서버)
- **경로**: `mongddang-papi/`
- **상태**: 준비 중
- **역할**:
  - 어드민 프론트엔드용 REST API 제공
  - 관리자 전용 비즈니스 로직 처리
  - 데이터베이스 연동
- **의존성**: `common` 모듈 사용

### 5. Common (공통 모듈)
- **경로**: `common/`
- **역할**:
  - 도메인 모델 정의 (Entity, DTO 등)
  - 공통 유틸리티 클래스
  - 공통 상수 및 설정
  - 공통 예외 처리
- **사용처**: `mongddang-api`, `mongddang-papi` 모듈에서 공통으로 사용

## 🔄 모듈 간 통신 구조

```
┌─────────┐         ┌─────────┐         ┌─────────┐
│  Front  │ ──API──>│mongddang│<───────>│ Common  │
│         │ /v1/*   │  -api   │         │         │
└─────────┘         └─────────┘         └─────────┘
                                              ▲
┌─────────┐         ┌─────────┐              │
│  Admin  │ ──API──>│mongddang│<──────────────┘
│         │         │  -papi  │
└─────────┘         └─────────┘
 (준비 중)          (준비 중)
```

### 통신 흐름
1. **Front → mongddang-api**: 사용자 요청을 `/v1/*` API로 전송
2. **Admin → mongddang-papi**: 관리자 요청을 mongddang-papi로 전송 (구현 예정)
3. **mongddang-api/mongddang-papi → Common**: 공통 도메인 모델 및 로직 사용

## 🛠️ 개발 환경 설정

### 사전 요구사항
- **Java**: 17 이상
- **Node.js**: 18 이상
- **Gradle**: 프로젝트에 포함됨

### 프로젝트 클론 및 설정

```bash
# 프로젝트 클론
git clone <repository-url>
cd gachamongddang

# 프론트엔드 의존성 설치
cd mongddang-front/web
npm install
```

## 🚀 개발 서버 실행

### 프론트엔드 개발 서버
```bash
cd mongddang-front/web
npm run dev
```
- 접속: http://localhost:5173

### API 서버 실행
```bash
./gradlew :mongddang-api:bootRun
```
- 접속: http://localhost:8080
- API: http://localhost:8080/v1/*

## 📦 빌드

### 전체 빌드
```bash
./gradlew build
```

### 개별 모듈 빌드
```bash
# 공통 모듈
./gradlew :common:build

# API 서버 모듈
./gradlew :mongddang-api:build
```

## 🚢 프로덕션 배포

### 배포 스크립트 사용
```bash
# 전체 빌드 (프론트엔드 + 백엔드)
./deploy.sh build

# 서버 시작
./deploy.sh start

# 서버 중지
./deploy.sh stop

# 서버 재시작
./deploy.sh restart

# 상태 확인
./deploy.sh status
```

자세한 배포 가이드는 [DEPLOY.md](./DEPLOY.md)를 참고하세요.

## 📁 프로젝트 구조 상세

```
gachamongddang/
├── mongddang-front/                # 프론트엔드
│   └── web/                        # React 애플리케이션
│           ├── src/
│           │   ├── api/            # API 클라이언트
│           │   ├── components/     # UI 컴포넌트
│           │   ├── pages/          # 페이지 컴포넌트
│           │   └── ...
│           └── package.json
│
├── mongddang-api/                  # API 서버
│   ├── src/
│   │   ├── main/
│   │   │   ├── kotlin/
│   │   │   │   └── com/meg/gachamongddang/server/
│   │   │   │       ├── ServerApplication.kt
│   │   │   │       ├── config/     # 설정 클래스
│   │   │   │       ├── controller/ # REST 컨트롤러
│   │   │   │       ├── service/    # 비즈니스 로직
│   │   │   │       └── repository/ # 데이터 접근
│   │   │   └── resources/
│   │   │       └── application.properties
│   │   └── test/
│   └── build.gradle.kts
│
├── mongddang-admin/                # 어드민 프론트엔드 (준비 중)
│   └── README.md
│
├── mongddang-papi/                 # 어드민 API 서버 (준비 중)
│   └── README.md
│
├── common/                         # 공통 모듈
│   ├── src/
│   │   └── main/kotlin/
│   │       └── com/meg/gachamongddang/common/
│   │           ├── domain/         # 도메인 모델
│   │           ├── dto/            # DTO
│   │           ├── util/           # 유틸리티
│   │           └── exception/      # 예외 처리
│   └── build.gradle.kts
│
├── build.gradle.kts                # 루트 빌드 설정
├── settings.gradle.kts             # 모듈 설정
├── deploy.sh                       # 배포 스크립트
└── README.md
```

## 📝 주요 기능

### 사용자 기능 (Front + mongddang-api)
- 🛍️ 상품 조회 및 검색
- 🛒 장바구니 및 위시리스트
- 💳 주문 및 결제
- 📦 배송 조회
- 👤 마이페이지

### 관리자 기능 (Admin + mongddang-papi) - 준비 중
- 📦 상품 관리
- 📊 주문 관리
- 👥 사용자 관리
- 📈 통계 및 분석

## 🔗 관련 문서

- [프로젝트 구조 상세](./PROJECT_STRUCTURE.md)
- [배포 가이드](./DEPLOY.md)
- [Front README](./mongddang-front/web/README.md)
- [Common 모듈](./common/README.md)

## 📄 라이선스

[라이선스 정보]
